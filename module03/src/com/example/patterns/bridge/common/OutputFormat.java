package com.example.patterns.bridge.common;

/** CSV is the format that was added later - see how each side of the example copes with it. */
public enum OutputFormat {
    PDF,
    EXCEL,
    HTML,
    CSV
}
